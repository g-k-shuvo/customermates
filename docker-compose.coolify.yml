services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20

  app:
    image: ${APP_IMAGE}
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - SERVICE_FQDN_APP_4000
      - APP_MODE=self-hosted
      - NODE_ENV=production
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - BASE_URL=${BASE_URL}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - CRON_SECRET=${CRON_SECRET}
      - RESEND_API_KEY=${RESEND_API_KEY}
      - RESEND_OPERATOR_EMAIL=${RESEND_OPERATOR_EMAIL}
      - EMAIL_TRANSPORT=${EMAIL_TRANSPORT}
      - WORKFLOW_TARGET_WORLD=@workflow/world-postgres
      - WORKFLOW_POSTGRES_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
    expose:
      - "4000"
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:4000/').then(r=>process.exit(r.status<500?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 120s

  cron:
    build:
      context: ./docker/cron
    restart: unless-stopped
    depends_on:
      app:
        condition: service_healthy
    environment:
      - APP_INTERNAL_URL=http://app:4000
      - CRON_SECRET=${CRON_SECRET}

volumes:
  postgres-data:
